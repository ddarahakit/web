# 빌드 컨텍스트 최소화 (프론트엔드 이미지 빌드용)
node_modules
dist
.git
.gitignore

# 환경파일은 이미지/빌드 컨텍스트에 포함하지 않는다.
# 운영값은 build-arg(VITE_*) 로 주입한다 → Dockerfile 의 ARG/ENV 참고.
.env
.env.*

# 인증서/키 (이미지에 포함 금지)
ssl

# 도커 관련
Dockerfile
.dockerignore
