spring:
  application:
    name: backend
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            redirect-uri: ${GOOGLE_REDIRECT_URI}
            scope:
              - email
              - profile
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-name: Kakao
            authorization-grant-type: authorization_code
            redirect-uri: ${KAKAO_REDIRECT_URI}
            scope: profile_nickname
            client-authentication-method: client_secret_post
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_ADDR}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          starttls:
            enable: true
            required: true
          auth: true
          timeout: 5000

  datasource:
    url: ${DB_URL}
    driver-class-name: org.mariadb.jdbc.Driver
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        # 지연 로딩(컬렉션/프록시)을 IN 절로 묶어 N+1 완화 (N건 → N/100 쿼리)
        default_batch_fetch_size: 100

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    org.springframework.boot.autoconfigure.security: DEBUG
    org.springframework.http: DEBUG

jwt:
  secret:
    key: ${JWT_SECRET}

portone:
  portone_secret: ${PORTONE_SECRET}

app:
  domain:
    server: "http://localhost"
    cookie: "localhost"
  aes:
    key: ${AES_SECRET_KEY:devAesSecretKey1234567890123456}  # 32자 (AES-256), 운영 환경에서 반드시 환경 변수로 교체
  token:
    access-max-age: 1800000  # 30분 (밀리초)
    refresh-max-age: 604800000  # 7일 (밀리초)
    # 로컬은 http://localhost 이므로 Secure 제거(있으면 브라우저가 쿠키 저장/전송 안 함), SameSite=Lax
    access-format: "ATOKEN=%s; HttpOnly; SameSite=Lax; Domain=localhost; Path=/; Max-Age=%d"
    refresh-format: "RTOKEN=%s; HttpOnly; SameSite=Lax; Domain=localhost; Path=/; Max-Age=%d"

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
      show-components: always
  health:
    db:
      enabled: true
    mail:
      enabled: false
    diskspace:
      enabled: false
    ping:
      enabled: false
    ssl:
      enabled: false
project:
  upload:
    path: ${UPLOAD_PATH}